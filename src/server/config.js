// Infusionsoft API url
const apiBaseUrl = 'http://work.fsportal.site/api.php';

// Email credentials for password reset (this account will send reset link)
const emailCredentials = {
  user: '',
  pass: '',
};

module.exports = {
  apiBaseUrl,
  emailCredentials,
};